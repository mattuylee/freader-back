export const globalConfig = require('./global-config')

export const entityTrimer = { trim: require('./entity-trimer') }
export const responseFilter = { filter: require('./response-filter') }


export const setPrototype = require('./prototype-setter')
export const prototypeSetter = { setPrototype: require('./prototype-setter') }

export const validate = require('./validatior')
export const validator = { validate: require('./validatior') }
